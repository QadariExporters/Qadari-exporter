'use client';
import Link from 'next/link';
import { ArrowRight, Minus, Plus, Trash2 } from 'lucide-react';
import { useInquiry } from './InquiryProvider';
export function InquiryList() { const { items, remove, link } = useInquiry(); if (!items.length) return <div className="empty-state inquiry-empty"><p>Your inquiry list is empty.</p><p className="muted">Add products as you browse, then send one considered message to the Qadri team.</p><Link className="button button-dark" href="/products">Browse collection <ArrowRight size={16} /></Link></div>; return <div className="inquiry-layout"><div className="inquiry-items">{items.map(({ product, quantity }) => <div className="inquiry-item" key={product.slug}><div className="inquiry-thumb"><div className={`asset-visual asset-${product.image}`} /></div><div><p className="eyebrow">{product.category}</p><h3>{product.name}</h3><p className="muted">Quantity: {quantity}</p></div><button onClick={() => remove(product.slug)} aria-label={`Remove ${product.name}`}><Trash2 size={17} /></button></div>)}</div><aside className="inquiry-summary"><p className="eyebrow">Your selection</p><h2>{items.length.toString().padStart(2, '0')} <em>pieces</em></h2><p>Send your selected products directly to the Qadri Exporters team for pricing, quantities and product information.</p><a className="button button-whatsapp" href={link} target="_blank" rel="noreferrer">Send inquiry on WhatsApp <ArrowRight size={16} /></a></aside></div>; }
