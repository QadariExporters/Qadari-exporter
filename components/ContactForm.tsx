'use client';
import { useState } from 'react';
export function ContactForm() { const [sent, setSent] = useState(false); const [error, setError] = useState(''); const submit = (event: React.FormEvent<HTMLFormElement>) => { event.preventDefault(); const form = event.currentTarget; const data = new FormData(form); if (!data.get('name') || !data.get('email') || !data.get('message')) { setError('Please complete the required fields.'); return; } setError(''); setSent(true); form.reset(); }; if (sent) return <div className="form-success"><p className="eyebrow">Thank you</p><h3>Your inquiry is ready to be shared.</h3><p>This form is currently a front-end enquiry capture. Our team contact details can be connected here once provided.</p><button className="text-link" onClick={() => setSent(false)}>Send another inquiry</button></div>; return <form className="contact-form" onSubmit={submit}>{[['name', 'Name *', 'text'], ['email', 'Email *', 'email'], ['phone', 'Phone', 'tel'], ['company', 'Company', 'text'], ['product', 'Product of interest', 'text']].map(([name, label, type]) => <label key={name}>{label}<input name={name} type={type} /></label>)}<label>Message *<textarea name="message" rows={5} /></label>{error && <p className="form-error">{error}</p>}<button className="button button-dark" type="submit">Send inquiry <span>↗</span></button></form>; }
